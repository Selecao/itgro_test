name: itgro_test
description: An itgro test Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  built_value: ^7.1.0
  built_collection: ^4.3.2
  dio: ^3.0.10
  flutter_html: ^0.11.1
  html: ^0.14.0+3
  flutter_mobx: ^1.1.0+2
  mobx: ^1.2.1+2
  provider: ^4.3.2+2


dev_dependencies:
  flutter_test:
    sdk: flutter

  mobx_codegen: ^1.1.0+1
  build_runner: ^1.10.2
  built_value_generator: ^7.1.0

# The following section is specific to Flutter.
flutter:

  uses-material-design: true

  assets:
    - assets/images/itgro_job.png